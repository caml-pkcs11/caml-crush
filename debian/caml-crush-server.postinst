#!/bin/sh
set -e

setup_pkcs11proxyd_user() {
        if ! getent passwd pkcs11proxyd >/dev/null; then
                adduser --quiet --system --home /var/lib/pkcs11proxyd --group pkcs11proxyd
        fi
}

if [ "$action" = configure ]; then
	setup_pkcs11proxyd_user
fi

#DEBHELPER#

exit 0
