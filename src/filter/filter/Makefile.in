all:
	ocamlfind ocamlopt -pp "camlp4o pa_macro.cmo   " -package "str,config-file,netplex" -I ../../bindings-pkcs11 -I ../backend -c filter_common.ml filter_actions.ml filter_configuration.ml filter.ml
clean:
	@rm -f *.cmi *.cmx *.o *.cmo *~ *.opt 
