all:
	ocamlfind ocamlopt -package "str,config-file,netplex" -I ../../bindings-pkcs11 -I ../backend -c filter_common.ml filter_actions.ml filter_configuration.ml filter.ml
clean:
	@rm -f *.cmi *.cmx *.o *.cmo *~ *.opt 
